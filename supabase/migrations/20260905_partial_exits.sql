-- Частичные выходы: список продаж внутри сделки.
-- Каждая продажа — {"d":"2026-09-05","qty":120.5,"price":2.5}
-- qty в токенах, price за токен. Повторный запуск безопасен.

alter table goals_trades
  add column if not exists exits jsonb not null default '[]'::jsonb;

-- У старых закрытых сделок список пуст: они считаются по exit_price,
-- как одна продажа всей позиции. Переносить ничего не нужно.
